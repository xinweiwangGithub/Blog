<% include header %>

<% posts.forEach(function(post){ %>
	<h2><%= post.title %></h2>
	<p>
		anthor: <%= post.name %>
		created: <%= post.time.minute %>
	</p>
	<p>
		<%= post.post %>
	</p>
<% }) %>

<% include footer %>
